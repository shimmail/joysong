# Install exactly once while either UAT virtual-host template is active.
limit_req_zone $binary_remote_addr zone=joysong_uat_login:10m rate=10r/m;

upstream joysong_uat_backend {
    server 127.0.0.1:8081;
    keepalive 32;
}
